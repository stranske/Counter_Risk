## Autofix diagnostic note

- Date (UTC): 2026-02-15
- PR: #104
- Gate run: 22035596071
- Head SHA: `d9a0e7a6ec176245029959a13ce59f22363ddd08`

### Observed context

- Failing jobs were reported at `Install dependencies` (`Python CI / typecheck-mypy` and `Python CI / python 3.11`).
- The head commit modified only:
  - `pyproject.toml`
  - `requirements.lock`
- After that commit, dependency pins were inconsistent across local dependency sources:
  - `.github/workflows/autofix-versions.env` expected `black==26.1.0` and `mypy==1.19.1`
  - `pyproject.toml` and `requirements.lock` pinned older `black==24.10.0` and `mypy==1.18.2`
  - `requirements-dev.lock` still pinned `ruff==0.15.0` while `pyproject.toml` required `ruff==0.15.1`

### Autofix action

Aligned dependency pins to remove cross-file conflicts:

- `pyproject.toml`
  - `black==26.1.0`
  - `mypy==1.19.1`
- `requirements.lock`
  - `black==26.1.0`
  - `mypy==1.19.1`
- `requirements-dev.lock`
  - `black==26.1.0`
  - `mypy==1.19.1`
  - `ruff==0.15.1`

### Local validation

- `python scripts/sync_dev_dependencies.py --check`: passed
- `python scripts/sync_test_dependencies.py`: passed
- `pytest -q tests/test_dependency_version_alignment.py`: passed

### Caveat

- Could not run the exact CI installer (`uv pip ...`) in this local runner because `uv` is not installed here.
