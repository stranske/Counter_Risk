## Autofix diagnostic note

- Date (UTC): 2026-02-15
- PR: #104
- Gate run: 22038601126
- Head SHA: `2c1677b14e17968a8e7774ba51ca21b2bbe38c9d`

### Observed failure context

- Reported failing jobs:
  - `Python CI / python 3.11` (`Finalize check results`)
  - `gate-summary` (`Enforce Gate success`)
- `gate-summary` is downstream and failed because `python-ci` reported failure.

### Local reproduction

- Re-ran repo checks locally on Python 3.11:
  - `python scripts/sync_dev_dependencies.py --check`
  - `python scripts/sync_test_dependencies.py`
  - `ruff check src tests`
  - `mypy src`
  - `pytest -q`
- All checks passed locally.

### Autofix applied

Adjusted `scripts/sync_dev_dependencies.py` check-mode output behavior for protected non-blocking drift:

- When drift is only from protected non-blocking pins (currently `black`), `--check` now:
  - exits `0` (unchanged),
  - prints only warning + ignored items, and
  - avoids printing the generic `Found N version updates` summary used for blocking drift.

This keeps strict failure behavior for blocking drift while reducing ambiguity for CI wrappers/finalizers that may key off generic drift text.

### Validation after change

- `python scripts/sync_dev_dependencies.py --check`: passed
- `python scripts/sync_test_dependencies.py`: passed
- `ruff check src tests`: passed
- `mypy src`: passed
