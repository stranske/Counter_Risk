## Autofix diagnostic note

- Date (UTC): 2026-02-15
- PR: #104
- Gate run: 22036318689
- Head SHA: `3344dacdbfaefb5c02309eff9fbaf89a3ae315b2`

### Observed context

- Reported failing jobs all failed in `Install dependencies`:
  - `Python CI / lint-ruff`
  - `Python CI / typecheck-mypy`
  - `Python CI / python 3.11`
- The failure set is scoped to jobs that provision Python 3.11 environments.
- This environment cannot access GitHub Actions run logs directly, so the exact pip/uv error text could not be retrieved.

### Autofix action

Applied a minimal pin rollback for formatter install compatibility in Python 3.11:

- `pyproject.toml`
  - `black==26.1.0` -> `black==24.10.0`
- `requirements.lock`
  - `black==26.1.0` -> `black==24.10.0`
- `requirements-dev.lock`
  - `black==26.1.0` -> `black==24.10.0`

No other tool pins were changed.

### Local validation

- `python scripts/sync_test_dependencies.py`: passed
- `pytest -q tests/test_dependency_version_alignment.py`: passed

### Caveat

- `python scripts/sync_dev_dependencies.py --check` reports expected drift versus `.github/workflows/autofix-versions.env` for `black` (`26.1.0`).
- `.github/workflows/**` is protected by repo policy in this run, so that pin file was not edited.
