## Autofix diagnostic note

- Date (UTC): 2026-02-15
- PR: #104
- Gate run: 22036058238
- Head SHA: `a6441821c4dd8498a5ffcc7dd537f40fa9805c54`

### Observed context

- Reported failing jobs all failed in `Install dependencies`:
  - `Python CI / lint-ruff`
  - `Python CI / typecheck-mypy`
  - `Python CI / python 3.11`
- The failing head had only dev-tool version bumps in:
  - `pyproject.toml`
  - `requirements.lock`
  - `requirements-dev.lock`
- Failure signature is consistent with Python 3.11 install incompatibility in the bumped dev-tool pin set.

### Autofix action

Rolled back dev-tool pins to the previous known-good set:

- `black==24.10.0`
- `ruff==0.15.0`
- `mypy==1.18.2`

Updated files:

- `pyproject.toml`
- `requirements.lock`
- `requirements-dev.lock`

### Local validation

- `python scripts/sync_test_dependencies.py`: passed
- `pytest -q tests/test_dependency_version_alignment.py`: passed
- `ruff check src tests`: passed
- `mypy src`: passed

### Caveat

- `python scripts/sync_dev_dependencies.py --check` reports drift versus `.github/workflows/autofix-versions.env` (`black==26.1.0`, `ruff==0.15.1`, `mypy==1.19.1`).
- In this environment, `.github/workflows/**` is protected and was not edited.
