## Autofix diagnostic note

- Date (UTC): 2026-05-11
- Gate run: `25696502853`
- PR: `#583`
- Head SHA: `531cae3ad08e80fb798a64aa16a33576692f9996`

### Observed context

- Reported failing jobs all failed in `Install dependencies`:
  - `Python CI / typecheck-mypy`
  - `Python CI / python 3.13`
  - `Python CI / python 3.12`
- Direct GitHub Actions logs were not accessible from this runner, so the exact installer error line could not be retrieved.
- Dependency pins included `black==26.3.1` in `pyproject.toml`, which is a likely resolver/install blocker for the shared install step.

### Autofix action

Applied a minimal rollback of Black to a known-good version:

- `pyproject.toml`: `black==26.3.1` -> `black==24.10.0`
- `requirements.lock`: `black==26.3.1` -> `black==24.10.0`
- `requirements-dev.lock`: `black==26.1.0` -> `black==24.10.0`

### Local verification

- `python scripts/sync_test_dependencies.py` -> passed
- `python -m pytest -q tests/test_dependency_version_alignment.py` -> passed

### Caveat

- `python scripts/sync_dev_dependencies.py --check` still reports drift because `.github/workflows/autofix-versions.env` currently pins `BLACK_VERSION=26.3.1`.
- That file is under `.github/workflows/**` and protected in standard agent mode, so it was not edited here.
