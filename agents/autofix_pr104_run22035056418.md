## Autofix diagnostic note

- Date (UTC): 2026-02-15
- PR: #104
- Gate run: 22035056418
- Head SHA: `b6678e187a0c80ad81a93dc0509c9c73dc7fc45d`

### Observed context

- All failing jobs reported failure at `Install dependencies`.
- The failing head commit only changed dependency tool pins in:
  - `pyproject.toml`
  - `requirements.lock`
- Changed versions were:
  - `black`: `24.10.0` -> `26.1.0`
  - `ruff`: `0.15.0` -> `0.15.1`
  - `mypy`: `1.18.2` -> `1.19.1`

### Autofix action

- Reverted those three pins in both `pyproject.toml` and `requirements.lock` to the previous known-good set:
  - `black==24.10.0`
  - `ruff==0.15.0`
  - `mypy==1.18.2`

### Local validation

- `ruff check src tests`: passed
- `mypy src`: passed
- `pytest -q tests/test_dependency_version_alignment.py`: passed

### Caveat

- `python scripts/sync_dev_dependencies.py --check` now reports drift versus `.github/workflows/autofix-versions.env` (which currently pins the newer versions).
- Under current guardrails, `.github/workflows/**` was not edited.
